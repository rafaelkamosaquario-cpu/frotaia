# Frota IA — Itens em aberto (atualizado e reconferido em 2026-09-18)

Lista consolidada de tudo que está pendente hoje, por categoria. Itens já fechados ficam marcados abaixo — o restante aguarda decisão ou execução. **A partir desta revisão, cada pendência em aberto traz uma linha "Como tratar" com o próximo passo concreto** — quem precisa agir (você, eu, ou os dois) e o que exatamente fazer.

## Já fechados

1. ~~`vehicles.current_odometer_km` como 4ª fonte de km~~ — ✅ feito 06/09 (commit `ab037d2`). O helper de manutenção por km agora também considera o campo manual do cadastro do veículo, além de abastecimento/pneu/manutenção.
2. ~~Testar prompt injection~~ — ✅ revisão estática do código feita 06/09; 1 lacuna real encontrada e corrigida (regra nova no `systemPrompt.ts` tratando conteúdo de imagem/PDF/página web sempre como dado, nunca instrução). **Teste comportamental ao vivo contra o modelo real continua em aberto**, se quiser fazer depois — precisa você logar no painel ou mandar teste pelo WhatsApp, não é algo que se faz só por revisão de código.
3. ~~Preço novo (Essencial/Pro)~~ — ✅ **implementado e no ar em 10/09/2026**. Catálogo virou Individual R$89,90 / Essencial R$149,90 (até 3 veículos) / Pro R$249,90 (até 10 veículos), todos com opção mensal (cartão) e anual (Pix ou cartão parcelado). Migration no Supabase, catálogo/checkout/tool do WhatsApp/landing page/limite de veículos (trigger de banco incluído) atualizados, 618 testes passando, deploy confirmado em produção sem erro. Detalhes técnicos completos em `docs/FROTA_IA_CHECKOUT_MERCADOPAGO_ATUAL.md` (seção 15).
4. ~~Inversão do funil — demo real antes do cadastro completo~~ — ✅ **implementado, commitado e publicado em 11/09/2026** (commit `1955b12`, branch `claude/frota-ia-assistente-setup-qlrbac`). Landing/WhatsApp agora abre com um menu pequeno (analisar frete/calcular rota/calcular custo/conhecer funções) em vez das 11+1 perguntas de cadastro; empresa mínima + trial criados em silêncio no primeiro contato; IA responde em modo restrito (só as ferramentas do track escolhido); cadastro completo só roda depois do pagamento confirmado. Lead "Empresas" mantém o fluxo de sempre, sem demo. 656 testes automatizados passando, tsc/eslint/build limpos. Migration do banco (`onboarding_state` +2 valores) já aplicada em produção via MCP. Detalhe completo em `FROTA_IA_JORNADA_POR_PLANO_2026-09-10.md` e `FROTA_IA_FLUXOGRAMA_ATUAL_2026-09-10.md`. **Falta**: confirmar o deploy no Railway e o teste manual ponta a ponta (itens 6 e 10 abaixo).
5. ~~Bug do redirect pós-login pra V1 em vez do Painel~~ — ✅ **corrigido e publicado em 18/09/2026** (commit `28e939e`, mesma branch). Você reportou que logar com Google em `https://frotaia.up.railway.app/` sempre caía "na primeira versão" — a senha pedida era a do próprio Google (`/login` não tem campo de senha nenhum), mas depois de autenticar o código mandava sempre pra `/` (a V1 antiga, chat de teste restrito a admin), nunca pro Painel novo. Corrigido: `/login` agora respeita um parâmetro `next` (padrão `/frota/dashboard`, não mais `/`), e `/frota/layout.tsx` passa esse `next` explicitamente ao redirecionar quem não está autenticado. tsc/eslint/656 testes/build limpos. **Falta**: confirmar deploy deste commit também no Railway (mesma pendência do item 6 abaixo).

## Aguardando sua decisão

6. **Deploy no Railway — confirmado indiretamente via banco de dados, ainda sem checar logs/status diretamente.** Pra inversão do funil (`1955b12`): consultei a tabela `onboarding_sessions` pelo Supabase MCP em 18/09/2026 e achei 2 sessões reais em produção no estado novo `awaiting_demo_input` (o estado só existe depois do deploy) — uma criada em 11/09/2026 16:34 (telefone `+55 42 9858-2489`, a mesma conta de teste "Frotaia" já usada em sessões anteriores) e outra em 13/09/2026 01:20 (`+55 42 9800-6380`). Isso prova que aquele código rodou de verdade em produção. Pro fix do redirect (`28e939e`, item 5 acima): ainda não há como confirmar pelo banco (é uma mudança só de front-end/rota, não deixa rastro em tabela nenhuma) — só dá pra confirmar testando de verdade no navegador ou olhando `list-deployments`/`get-logs` do Railway, nenhum dos dois feito ainda.
   **Como tratar**: peça pra eu rodar `list-deployments`/`get-logs` do Railway via MCP na próxima sessão (2 minutos, resolve de vez) — ou você mesmo confere em Railway → serviço `frota-ia-assistente` → aba Deployments, olhando se o último deploy bate com o commit `0a76268` (o mais recente) e está com status "Success".
7. **Pedágio via Maplink** — decidido em 02/08/2026 que seria por aí (não pela Routes API do Google), mas nada foi implementado. Falta você criar a conta na Maplink pra eu confirmar o schema real da API antes de codar.
   **Como tratar**: ação seu — crie a conta na Maplink e contrate a Toll API; assim que tiver acesso à documentação/credenciais, me chame que eu confirmo o schema real de request/response antes de escrever qualquer código (nunca implemento contra API sem isso verificado).
8. **Patch "staged" pendente no Railway desde 26/08/2026** (34 mudanças, nunca aplicado nem descartado) — investigado em 10/09: a própria API do Railway classifica como não-destrutivo, provável sobra de sessão antiga sem efeito real. Ainda assim, melhor descartar pra limpar — só dá pra fazer no painel web do Railway (eu não tenho ferramenta pra isso), passos já passados a você, ainda não confirmado se foi feito.
   **Como tratar**: ação sua, 1 minuto — entre em Railway → serviço `frota-ia-assistente` → aba de mudanças pendentes/staged changes e clique em descartar (discard). Não tem risco: a própria API já classificou como não-destrutivo.
9. **`frotaia.app.br` não resolve DNS** (achado em 10/09/2026, `ERR_NAME_NOT_RESOLVED`) — o domínio de serviço `frotaia.up.railway.app` funciona normal, então não é urgente, mas o domínio próprio configurado no Railway está fora do ar. Provável configuração pendente no registrador do domínio — fora do que eu consigo corrigir por código.
   **Como tratar**: ação sua — confira no registro.br se o registro DNS (CNAME ou A, conforme o Railway pedir em Settings → Domains do serviço) pro domínio raiz `frotaia.app.br` está criado corretamente; se precisar, me manda print da tela de Domains do Railway que eu confirmo qual registro exato falta.
10. **Checkout e demo novos não testados com clique/mensagem real** — os 9 planos/formas de cobrança estão cobertos por teste automatizado e a rota `/assinar` responde normal em produção, mas nenhum link de checkout real chegou a ser gerado e conferido no Mercado Pago (tentativa em 10/09 interrompida por instabilidade do navegador; você optou por confiar nos testes automatizados por ora). Isso agora também vale pro fluxo de demo inteiro (item 4): um número novo real batendo no webhook, escolhendo um track, recebendo o resultado, tocando "Ver planos" e confirmando que o pagamento de verdade dispara o cadastro completo. Vale fazer esse teste ponta a ponta (você mesmo, ou comigo) antes de divulgar a nova jornada pra cliente de verdade.
    **Como tratar**: combine comigo uma sessão de teste — você manda uma mensagem de um número de WhatsApp que **nunca usou o Frota IA antes** (o trial só libera 1x por telefone), eu acompanho pelos logs do Railway e pelo banco em tempo real, seguimos a demo até "Ver planos" e fazemos 1 pagamento pequeno de teste de verdade no Mercado Pago pra confirmar que o cadastro completo dispara sozinho depois da aprovação.
11. **As 2 sessões reais de demo (item 6) nunca avançaram até o pagamento — e o primeiro trial já venceu de fato** (reconfirmado em 18/09/2026 às 19:13 UTC, consulta direta ao Supabase). Ambas as empresas "Minha operação" seguem paradas no estado `awaiting_demo_input` — nenhuma clicou "Ver planos" nem chegou a gerar veículo/cadastro completo. `trial_usage` confirma 1 teste por telefone já consumido nos dois números. Prazo do trial (`subscriptions.valido_ate`): a sessão de 11/09 (`+55 42 9858-2489`) **já passou do prazo** (venceu 18/09 às 16:34 UTC — `isAccessAllowed()` já bloqueia o acesso dessa empresa agora, mesmo o campo `status` no banco ainda mostrando "TRIAL" por enquanto — ele só vira "EXPIRADA" na próxima leitura real via app, correção reativa, não um cron); a de 13/09 (`+55 42 9800-6380`) ainda tem prazo até 20/09/2026. Se forem testes seus e você quiser continuar testando depois do vencimento, vai precisar de um número novo — `trial_usage` bloqueia reuso do mesmo telefone mesmo que a empresa seja excluída.
    **Como tratar**: se esses 2 números eram só teste interno seu, não é uma pendência de produto de verdade — é só confirmação de que ninguém de fora ainda passou pela demo. Se quiser repetir o teste, use um número de WhatsApp novo (nunca cadastrado antes) — pode aproveitar o mesmo teste do item 10 acima, não precisa de 2 rodadas separadas.

## Riscos técnicos conhecidos, sem correção pedida ainda

12. **Chargeback/estorno do plano anual** — zero tratamento no código. Se um pagamento anual for estornado depois de aprovado, o acesso ao Painel não é revogado automaticamente.
    **Como tratar**: decisão sua — se quiser fechar esse risco, eu implemento um handler que escuta o evento de chargeback/estorno do Mercado Pago (mesmo webhook que já existe) e revoga `fleet_panel_included`/muda `status` pra `CANCELADA` automaticamente. Não é urgente enquanto o volume de clientes reais for baixo (hoje é zero).
13. **Webhook do WhatsApp sem teste de rota dedicado** — mitigado parcialmente em 11/09/2026: o bloco novo da inversão do funil (demo pré-cadastro) ganhou cobertura de teste própria dentro de `src/app/api/whatsapp/webhook/route.test.ts`, mas o restante da rota (mídia, checklist, grupo etc.) continua sem suíte dedicada — risco médio de regressão silenciosa em mudança futura nesses trechos.
    **Como tratar**: se quiser reduzir esse risco, posso escrever a suíte de teste faltante numa próxima sessão dedicada (não é uma tarefa de 5 minutos, é trabalho de algumas horas). Não bloqueia nada hoje, só protege contra bug futuro nessas partes da rota.
14. **Sem aviso automático antes do plano anual vencer** — hoje só o período de teste grátis (TRIAL) tem esse lembrete automático.
    **Como tratar**: se quiser, crio um cron novo (mesmo padrão do aviso de trial, `frotaia-trial-avisos-cron`) que avisa X dias antes do `valido_ate` de assinaturas anuais. Simples de implementar quando você decidir a antecedência ideal (ex.: 15 e 3 dias antes).
15. **Tokens estáticos na URL do webhook/crons** — funciona, mas é uma fraqueza arquitetural conhecida (não é vazamento comprovado).
    **Como tratar**: migrar pra assinatura HMAC por requisição (mesmo padrão já usado no webhook do Mercado Pago) resolveria de vez, mas é uma mudança de arquitetura não-trivial — só vale priorizar se você quiser elevar o nível de segurança geral do sistema. Sem correção pedida até agora.
16. **Falta de HMAC no webhook do WhatsApp** — limitação da própria Z-API (provedor não-oficial), não é algo corrigível sem trocar de provedor.
    **Como tratar**: não há correção de código possível aqui. A única forma de resolver de verdade é migrar pra uma API oficial do WhatsApp Business — decisão de produto/custo bem maior, não uma tarefa pontual. Registrado como risco aceito por ora.
17. **Conta criada por número de telefone, sem OTP** — qualquer um que tenha acesso físico/temporário ao WhatsApp de alguém pode iniciar um cadastro nesse número.
    **Como tratar**: implementar um código de verificação (SMS ou mensagem própria) antes de liberar o primeiro acesso resolveria — não implementado ainda porque nunca foi pedido; avalie se o risco compensa a fricção extra no onboarding antes de decidir.

## Dívida técnica / conteúdo, baixa prioridade

18. **Enum morto `awaiting_vehicle_count`** — sobra de um fluxo antigo, sem uso real, cosmético.
    **Como tratar**: nenhuma ação necessária — Postgres não permite remover valor de enum, e o valor já não é usado em lugar nenhum do código. Ignorar.
19. **Conteúdo dos 5 arquivos de `src/ai/conhecimentos/*.md`** (negociação, manutenção preventiva, pneus/direção econômica, gestão/indicadores, jornada/bem-estar) — ainda é a primeira versão (rascunho meu), nunca revisado com calma pra tom/precisão.
    **Como tratar**: quando tiver um tempo livre, revisamos juntos — você aponta o que soa errado/genérico demais e eu reescrevo o trecho. Não precisa ser tudo de uma vez, dá pra ir por arquivo.
20. **4 chaves antigas do enum `subscription_plan`** (`MENSAL`, `GESTAO_MENSAL`, `ANUAL_PARCELADO`, `ANUAL_PIX`) ficaram órfãs desde a reestruturação de 10/09 — Postgres não deixa remover valor de enum, então continuam existindo no banco sem uso. Cosmético, não afeta nada. Reconfirmado direto no enum via Supabase MCP em 18/09/2026 (ainda as mesmas 4 chaves, nenhuma nova).
    **Como tratar**: nenhuma ação necessária, mesmo caso do item 18 — ignorar.
21. **Pré-seleção automática de plano no CTA pós-demo** — hoje o CTA "Ver planos" sempre mostra os 3 botões (Individual/Essencial/Pro), mesmo quando o cliente veio de um CTA de plano específico da landing (`ofertaPretendida` já salva desde o primeiro contato). Daria pra pré-selecionar/pular direto pro plano certo — não implementado nesta rodada, mencionado como possível polimento futuro.
    **Como tratar**: se quiser, implemento isso numa próxima sessão — é uma mudança pequena e localizada (o dado `ofertaPretendida` já existe, só falta usar pra pular a pergunta). Peça quando quiser priorizar.
22. **`DashboardClient.tsx` (versão antiga do Dashboard) ficou órfão** (achado em 18/09/2026, ao revisar o polimento do painel do commit `0a76268`) — o Dashboard foi reescrito num componente novo (`DashboardOverview.tsx`), e o arquivo antigo (com as 2 variantes visuais `?cardStyle=a/b`) não é mais importado por nada no projeto. Junto com ele, o parâmetro de URL `?cardStyle=a/b` em `page.tsx` também ficou morto — ainda é calculado e passado como prop, mas o componente novo nunca usa esse valor. Nenhum efeito funcional visível, cosmético, sem correção pedida.
    **Como tratar**: posso apagar o arquivo morto e limpar o parâmetro de URL numa próxima sessão de organização de código — leva minutos, sem risco, só não é urgente porque não afeta nada em produção.

## Também registrado, sem ação pedida

- **Ajustar exageros comerciais** de uma versão antiga do documento comercial ("segurança jurídica", "o produto se paga sozinho", "zero digitação") — o documento comercial novo (`FROTA_IA_COMERCIAL_ATUAL_2026-09-06.md`) já foi escrito sem essas frases, mas a versão antiga (`FROTA_IA_COMERCIAL_E_APRESENTACAO_DO_PRODUTO.md`) ainda existe no repo com o texto antigo, caso queira que eu corrija ou apague.
  **Como tratar**: me diga se quer que eu corrija o texto antigo ou apague o arquivo de vez — qualquer uma das duas é rápida, só esperando sua decisão.

## Reconferido em 18/09/2026 — auditoria completa cruzando código, banco e produção

Nesta data, além das atualizações acima, foi feita uma auditoria completa cruzando os 9 documentos desta pasta contra: o repositório GitHub (`git fetch`, sem commit novo além do `0a76268` já registrado), o banco de produção via Supabase MCP (estado de `onboarding_sessions`/`subscriptions` idêntico ao já descrito no item 11 — nenhuma sessão nova, nenhuma conversão), e o código real do onboarding/webhook do WhatsApp (`demoConversation.ts`, `webhook/route.ts` — confirmados intocados desde a última auditoria, batendo 100% com `FROTA_IA_JORNADA_POR_PLANO_2026-09-10.md` e `FROTA_IA_ONBOARDING_WHATSAPP_2026-09-11.md`). **Nenhuma divergência nova encontrada** entre os documentos e o produto real — os outros 7 PDFs da pasta (Comercial, Como a IA Atua, Ferramentas, Fluxograma, Jornada por Plano, Onboarding-simulação, V1/V2) seguem descrevendo fielmente o que está em produção hoje, sem necessidade de reescrita de conteúdo.
